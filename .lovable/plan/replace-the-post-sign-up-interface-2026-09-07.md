# Replace the post-sign-up interface

## What will change
- Keep the existing sign-in and registration form.
- After a new account is created, continue sending the user to their profile.
- Restyle the profile to match the supplied reference: centered brand navigation, lavender banner, lime initial avatar, name and handle, item/outfit/lookbook tabs, search controls, QR area, and three-phone visual.
- Make the profile layout work cleanly on desktop and mobile while preserving live account counts and existing links.

## Technical details
- Adjust the signed-in page frame so the profile can use its own full-width reference layout without the standard wardrobe navigation competing with it.
- Update the profile presentation using the existing semantic color and typography system.
- Add complete page metadata and verify the signed-in profile in the live preview.
